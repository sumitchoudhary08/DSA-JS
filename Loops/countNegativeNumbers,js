//WAP to find the count of negative numbers.

const arr = [12, -3, 9, -1, -2, 4];

const negativeNumbersCount = () => {
  let count = 0;
  for (let i = 0; i < arr.length; i++) {
    if (arr[i] < 0) count++;
  }
  return count;
};

var result = negativeNumbersCount(arr);

console.log(result);
